## Terminé
- Lot 0, Lot 1, Lot 2 — complets (voir 6.md)
- Lot 3, étape 3.1 — entité `Etape` (titre, description, dateEcheance, ordre,
  statut, projet) + `EtapeRepository`
  - Reutilise l'enum `StatutEtape` deja cree au lot 1
  - Table `etape` pas encore migree (prevu a l'etape 3.14, comme pour le lot 2)
- Lot 3, étape 3.2 — `EtapeDto`, `EtapeRequest`, `SoumissionRequest`
  - Entite `Etape` completee avec les champs de soumission/validation
    (lienLivrable, commentaireSoumission, dateSoumission,
    commentaireValidation, dateValidation), necessaires pour ces DTOs
- Lot 3, étape 3.3 — `EtapeService` : creation (encadrant), soumission (chef
  d'equipe), validation (encadrant), CRUD (modifier/supprimer si pas encore
  soumis). Calcul EN_RETARD (EF-26) via `@Scheduled` (cron quotidien 1h) ;
  `@EnableScheduling` ajoute sur `PfeBackendApplication`.

- Lot 3, étape 3.4 — `EtapeController` :
  - `GET/POST /api/projets/{projetId}/etapes` (lister, creer — ENCADRANT)
  - `GET/PUT/DELETE /api/etapes/{id}` (ENCADRANT pour ecriture)
  - `PATCH /api/etapes/{id}/soumettre` (ETUDIANT, chef d'equipe)
  - `PATCH /api/etapes/{id}/valider` (ENCADRANT), reutilise `DecisionSujetRequest`
    plutot qu'un nouveau DTO (le plan 3.2 n'en prevoyait pas)
  - Pas de modification de SecurityConfig : `/api/**` deja authentifie par
    defaut, roles geres par `@PreAuthorize` comme les autres controleurs

## Terminé (suite)
- Bloc A (3.1 a 3.5) — commit "feat: jalons (entite, DTOs, service,
  controleur) + tests Postman" pousse sur origin/main
- Lot 3, étape 3.6 — DTOs Bloc B : `StatsEtudiantDto`, `StatsEncadrantDto`,
  `StatsAdminDto` (pas de mapping depuis une seule entite : construits par
  EtapeService/futur StatsService a l'etape 3.7)

- Lot 3, étape 3.7 — `StatsService` + `StatsController` :
  - `GET /api/stats/etudiant` (ETUDIANT) : jalons de son projet (total,
    valides, en retard, restants, prochaine echeance). Objet "vide" (pas
    d'erreur 404) si l'etudiant n'a pas encore de projet.
  - `GET /api/stats/encadrant` (ENCADRANT) : sujets en attente, projets en
    cours, jalons a valider / en retard, tous projets confondus
  - `GET /api/stats/admin` (ADMINISTRATEUR) : compteurs globaux
  - Nouvelles methodes repository : `EtapeRepository.countByProjetId`,
    `.findFirstByProjetIdAndStatutNotOrderByDateEcheanceAsc`,
    `.countByProjetEncadrantIdAndStatut` (traversee 2 niveaux),
    `.countByStatut` ; `SujetRepository.countByStatutIn` /
    `.countByEncadrantIdAndStatutIn` ; `ProjetRepository.countByStatut`

- Bloc B (3.6 a 3.8) — teste, commit "feat: stats par role
  (etudiant/encadrant/admin)" pousse (merge a6fcc25 inclus)
- Lot 3, étape 3.9 — `EtudiantJalonsView.vue` : liste des jalons du projet
  (statut, echeance, livrable/commentaire soumis, retour de validation) +
  dialogue de soumission (lien + commentaire), reserve au chef d'equipe
  - `LIBELLES_STATUT_ETAPE` / `COULEURS_STATUT_ETAPE` ajoutes a
    `utils/statuts.js`
  - Pas encore reliee au routeur ni au dashboard : prevu groupe a l'etape
    3.12 avec les vues encadrant (3.10) et admin (3.11)

- Lot 3, étape 3.10 — `EncadrantJalonsView.vue` : selection du projet
  (v-select, un encadrant peut en avoir plusieurs), CRUD jalons (creer,
  modifier/supprimer si pas encore soumis), validation d'un jalon SOUMISE
  avec commentaire optionnel

- Lot 3, étape 3.11 — `AdminStatsView.vue` : 8 tuiles KPI depuis
  `GET /stats/admin` (etudiants, encadrants, equipes, sujets, sujets en
  attente, projets, projets en cours, jalons en retard) ; tuiles "attention"
  (sujets en attente / jalons en retard) coloriees seulement si > 0

- Lot 3, étape 3.12 — routeur + cartes dashboard :
  - `router/index.js` : `/etudiant/jalons`, `/encadrant/jalons`, `/admin/stats`
  - `DashboardEtudiant.vue` : carte "Jalons" ajoutee
  - `DashboardEncadrant.vue` : carte "Jalons à valider" -> "Jalons", reliee
    (la carte "Projets encadrés" reste en placeholder, pas de vue construite
    pour ça dans 3.9-3.11)
  - `DashboardAdmin.vue` : carte "Statistiques" reliee a `/admin/stats`
  - Verification : `npm run build` echoue dans mon environnement (probleme
    de binding natif rolldown, node_modules installes sous Windows, sans
    rapport avec le code) ; verifie a la place chaque .vue via
    `@vue/compiler-sfc` (parse + compileScript + compileTemplate, tout OK)
    et `node --check` sur les fichiers JS modifies

- Lot 3, étape 3.13 — Test navigateur (Vous), verifie sans erreur : les 3
  dashboards + parcours complets (creation/soumission/validation jalon,
  stats admin)
- Lot 3, étape 3.14 — `db/migration/V2__jalons.sql` (table `etape`) +
  `ddl-auto` repasse sur `validate`
  - ATTENTION avant de relancer le backend : la table `etape` existe deja
    localement (creee via ddl-auto=update pendant 3.1-3.13). Flyway va
    executer V2 pour de vrai (pas de baseline comme pour V1) et echouera sur
    "relation etape already exists" si elle n'est pas supprimee d'abord.
    Se connecter a pfe_db et faire `DROP TABLE etape;` avant de redemarrer.

## Hors plan Lot 3 (a la demande de Zakaria)
- `AdminReferentielView.vue` : gestion filieres/promotions (onglets, CRUD),
  backend deja pret depuis le lot 2.1 (FiliereController/PromotionController)
  - route `/admin/referentiel`, carte "Filières et promotions" du dashboard
    admin reliee
- Gestion des utilisateurs (EF-03), backend + frontend cette fois (rien
  n'existait) :
  - `UtilisateurController` (`GET /api/admin/utilisateurs?role=`,
    `PATCH /api/admin/utilisateurs/{id}/actif`) + `UtilisateurService`
  - Scope volontairement limite a lister + activer/desactiver (jamais de
    suppression, EF-03) : pas d'edition des champs (nom/email/mdp), c'est
    plus sensible qu'un simple CRUD comme filieres/promotions
  - Un admin ne peut pas desactiver son propre compte (verifie backend et
    masque cote frontend)
  - `AdminUtilisateursView.vue`, route `/admin/utilisateurs`, carte
    "Utilisateurs" du dashboard admin reliee
  - `/api/admin/**` est deja proteges ADMINISTRATEUR par SecurityConfig
    (regle generique existante, jamais utilisee avant) ; `@PreAuthorize`
    ajoute quand meme par methode pour rester coherent avec les autres
    controleurs

## Étape en cours
Lot 3, étape 3.15 — Vérification, commit, tag v0.3-lot3 (Vous). A ajouter au
commit : `AdminReferentielView.vue`, `router/index.js`, `DashboardAdmin.vue`
(deja modifie pour /admin/stats, diff cumule).

## (archive) 3.5 — Test Postman (Vous). Collection preparee :
`docs/postman/Lot3-BlocA-Jalons.postman_collection.json`.
- A confirmer avant de lancer : qui est reellement chef de l'equipe du
  premier projet de karim.benali (variable emailEtudiantChef, par defaut
  yassine.alami@pfe.local). Le 2e item du dossier "1. Jalons" (GET equipe)
  affiche chefNom dans la console Postman pour verifier.
- EN_RETARD (EF-26) non testable via cette collection : pas d'endpoint HTTP,
  job planifie seulement (cron quotidien 1h).

## Point d'attention
- Confirme au run : `SchemaManagementException: missing table [etape]` avec
  `ddl-auto=validate`. Corrige en repassant `ddl-auto=update` dans
  `application.properties` (comme au lot 2). A remettre sur `validate` +
  migration Flyway V2__jalons.sql a l'etape 3.14.
- Backend redemarre correctement (table `etape` creee), confirme par Zakaria.

## Convention git
- Pas de commit a chaque sous-etape (3.1, 3.2, ...). Commit aux points de
  synchronisation naturels : fin de bloc (ex. fin du bloc A backend, avant le
  test Postman 3.5) ou fin de lot, comme au lot 2.
