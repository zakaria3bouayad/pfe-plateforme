## Terminé
- Lot 0, Lot 1, Lot 2 — complets (voir 6.md)
- Lot 3 — COMPLET (étapes 3.1 à 3.15), backend + frontend + Flyway
  Tag git : v0.3-lot3

  **Bloc A — Jalons**
  Entité `Etape` (titre, description, échéance, ordre, statut, projet, plus
  champs de soumission/validation) + repository. `EtapeService` : création
  par l'encadrant, soumission par le chef d'équipe (lien + commentaire),
  validation par l'encadrant, CRUD tant que pas soumis. Calcul automatique
  du statut EN_RETARD (EF-26) via job planifié quotidien. `EtapeController`
  (CRUD + /soumettre + /valider). Testé Postman.

  **Bloc B — Tableaux de bord**
  `StatsService`/`StatsController` : stats par rôle — étudiant (avancement
  de son projet), encadrant (sujets en attente, projets en cours, jalons à
  valider/en retard), admin (compteurs globaux). Testé Postman.

  **Bloc C — Frontend**
  Vue jalons étudiant (soumission), vue jalons encadrant (création +
  validation, multi-projets), vue statistiques admin (tuiles KPI), routeur
  et cartes dashboard reliées pour les 3 rôles. Testé navigateur.

  **Bloc D — Clôture**
  Migration Flyway `V2__jalons.sql`, `ddl-auto` repassé sur `validate`.

  **Hors plan (ajouté à la demande de Zakaria)**
  Gestion admin filières/promotions (CRUD, backend du lot 2.1 réutilisé),
  gestion des comptes utilisateurs (lister + activer/désactiver, jamais de
  suppression — EF-03 — ni d'édition des champs sensibles), liste de tous
  les projets avec filtre par statut.

## Étape en cours
Lot 4 (documents versionnés) — pas encore démarré.

## Dette technique connue
- Dashboard encadrant : carte "Projets encadrés" toujours en placeholder,
  aucune vue dédiée construite.
- Gestion utilisateurs : pas d'édition de profil ni de suppression côté
  admin, seulement activer/désactiver.
- EN_RETARD (EF-26) dépend uniquement du job planifié quotidien (1h du
  matin) : aucun endpoint pour le déclencher manuellement ou le tester à la
  demande.
- `GET /api/projets` reste accessible à tout compte authentifié (pas
  seulement l'admin) : comportement hérité du lot 2, pas modifié ici.
